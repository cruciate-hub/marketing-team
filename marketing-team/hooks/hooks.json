{
  "description": "Auto-sync the cruciate-hub marketplace on session start",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v claude >/dev/null 2>&1 || exit 0; claude plugin marketplace update cruciate-hub && claude plugin update marketing-team@cruciate-hub",
            "timeout": 30,
            "statusMessage": "Syncing marketing-team plugin…"
          }
        ]
      }
    ]
  }
}
