{
  "_doc": "Field map for the social.plus Glossary collection. STATUS: STUB — the pipeline is wired but every field slug except the Webflow built-ins name/slug is UNCONFIRMED (null). The publisher refuses to publish and --dry-run fails until the nulls are replaced with the real slugs. Do NOT fill in plausible-looking slugs.",
  "_status": "UNCONFIRMED — CONFIRM WITH STEFAN. Read the real slugs from the Webflow Designer (Glossary collection → Settings → fields) or from `GET https://api.webflow.com/v2/collections/66e2765d540e1939a89db93e` (Authorization: Bearer $WEBFLOW_API_TOKEN), then replace each null below. Keys starting with '_' are documentation and ignored by scripts/webflow_fieldmap.py.",
  "schema_version": 1,
  "collection": "glossary",
  "display_name": "Glossary",
  "site_id": "66e2765d540e1939a89db4bb",
  "collection_id": "66e2765d540e1939a89db93e",
  "_collection_id_provenance": "website/pages-glossary.json _meta.collectionId (generated by the site-JSON worker via the Webflow Data API, 76 items). Confirmed data, not a guess.",
  "live_url_prefix": "https://www.social.plus/glossary/",

  "fields": {
    "title": "name",
    "slug": "slug",
    "body": null,
    "_body_confirm": "CONFIRM WITH STEFAN — the RichText field slug that holds the entry body (definition paragraph + the five H2 sections). This is the first slug to confirm; dry-run fails on it alone.",
    "intro": null,
    "_intro_confirm": "Leave null if the Glossary has no separate summary/definition field; the definition paragraph then stays as the first <p> of the body. Set it to the field slug if such a field exists.",
    "date": null,
    "_date_confirm": "CONFIRM WITH STEFAN — a published-date DateTime field, if the collection has one; null skips it."
  },

  "metadata": {
    "Meta description": {"slug": null, "required": true, "max_length": 160,
                         "_confirm": "CONFIRM WITH STEFAN — SEO meta description PlainText slug."},
    "Alt text":         {"slug": null, "required": false,
                         "_confirm": "CONFIRM WITH STEFAN — only if the collection has an image + alt-text field; otherwise delete this entry."},
    "Category":         {"slug": null, "required": false, "type": "reference", "taxonomy": "categories",
                         "_confirm": "CONFIRM WITH STEFAN — glossary-content currently borrows the Blog Main Category Tag list as a placeholder. Confirm whether Glossary has its own taxonomy (fill taxonomies.categories with its item IDs), reuses Blog's (copy from blog.json), or has none (delete this entry)."}
  },

  "defaults": {},

  "taxonomies": {
    "categories": {},
    "_confirm": "CONFIRM WITH STEFAN — see metadata.Category._confirm."
  },

  "images": [],
  "_images_confirm": "CONFIRM WITH STEFAN — none known. If the Glossary collection has image fields, add {role, slug, variant, width, height} entries here (exact pixel sizes) and set inline_images.",
  "inline_images": null,

  "slug_rules": {
    "strip_years": true,
    "strip_leading_count": false,
    "_doc": "Glossary rewrites keep the existing live slug (glossary-content/SKILL.md 'Rewriting an existing entry'); pass it via the draft's `Slug:` line."
  },

  "checks": {
    "require_internal_links": true,
    "_require_internal_links_doc": "Related Terms must carry ≥2 internal links (compliance.py FAILs otherwise), so a converted entry with zero internal links means the section was lost.",
    "forbidden_body_strings": ["[fill before publish]"]
  }
}
